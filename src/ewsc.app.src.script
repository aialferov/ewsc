{application, ewsc, [
    {description, "Erlang WebSocket client library"},
    {vsn, "0.2.0"},
    {modules, []},
    {applications, [kernel, stdlib, cpflib, wsock]},
    {env, [
        {git_sha, lists:sublist(os:cmd("git rev-parse HEAD 2>/dev/null"), 8)}
    ]}
]}.
